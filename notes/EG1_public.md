# EG1 Public Note

The `EG1` package establishes the positive projected response floor.

Define the projected response operator `E_tau = Pi_resp L_tau Pi_resp` on the admissible response sector.

Raw theorem constant:

`kappa_realization^(raw) := c_realization_raw * realization_density_raw - e_realization_raw`.

Closure criterion:

- the projected response is finite and theorem-level,
- `kappa_realization > 0` on the declared admissible tube.

Main interpretation: the admissible closure cannot collapse because the projected realization response remains strictly positive.
